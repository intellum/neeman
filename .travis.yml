language: swift
osx_image: xcode10
xcode_sdk: iphonesimulator12.0
xcode_project: Neeman.xcodeproj
xcode_scheme: Neeman

branches:
 only:
 - master

 script:
  - xcodebuild -scheme "$TRAVIS_XCODE_SCHEME" -project "$TRAVIS_XCODE_PROJECT" -sdk "$TRAVIS_XCODE_SDK" test
